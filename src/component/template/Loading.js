import React from "react";

export default () => (
    <div className="loading">
        <button className="btn btn-primary">
            <i className="fa fa-refresh fa-spin"></i>&nbsp;
            Pegando sua posição atual.
        </button>
    </div>
)